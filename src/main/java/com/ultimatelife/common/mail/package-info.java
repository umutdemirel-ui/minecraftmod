/**
 * Mail / delivery system.
 *
 * <p>In-game mail boxes, parcels and delivery notifications. Persistent state is server side; the client receives snapshots over {@code com.ultimatelife.common.network}.</p>
 *
 * <p>Not implemented in Phase 1 - this package only documents where the system belongs so that it is
 * built in the right place later. No placeholder content ships here.</p>
 */
package com.ultimatelife.common.mail;
