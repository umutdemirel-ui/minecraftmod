/**
 * Vehicle system.
 *
 * <p>Drivable entities (cars, bikes, boats) with their physics and input handling. Input arrives as server-bound packets and is validated against server-side state before it is applied.</p>
 *
 * <p>Not implemented in Phase 1 - this package only documents where the system belongs so that it is
 * built in the right place later. No placeholder content ships here.</p>
 */
package com.ultimatelife.common.vehicle;
