/**
 * Furniture system (common).
 *
 * <p>Reserved for Phase 2+: furniture blocks, their block entities, sit/interaction logic and
 * the data that describes each furniture type. Nothing is implemented in Phase 1 - the
 * package exists so the system has a fixed home and its dependencies stay local.
 *
 * <p>Rules: server-authoritative state only, no direct references into
 * {@code com.ultimatelife.client}, registration goes through
 * {@code com.ultimatelife.common.registry}.
 */
package com.ultimatelife.common.furniture;
