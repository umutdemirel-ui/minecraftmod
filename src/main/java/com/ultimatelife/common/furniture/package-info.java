/**
 * Furniture system.
 *
 * <p>Multi-block and single-block furniture: seats, tables, shelves, lighting. Blocks are registered through {@code com.ultimatelife.common.registry.ModBlocks}, block entities through {@code ModBlockEntities} and their renderers under {@code com.ultimatelife.client.render} - the system itself never touches client classes.</p>
 *
 * <p>Not implemented in Phase 1 - this package only documents where the system belongs so that it is
 * built in the right place later. No placeholder content ships here.</p>
 */
package com.ultimatelife.common.furniture;
