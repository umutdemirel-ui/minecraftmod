/**
 * Economy system.
 *
 * <p>Currency, accounts, prices and transactions. All balances live on the server and are the single source of truth: clients only ever display values the server sent them.</p>
 *
 * <p>Not implemented in Phase 1 - this package only documents where the system belongs so that it is
 * built in the right place later. No placeholder content ships here.</p>
 */
package com.ultimatelife.common.economy;
