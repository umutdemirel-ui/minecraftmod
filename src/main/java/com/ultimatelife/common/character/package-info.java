/**
 * Character system (common).
 *
 * <p>Reserved for Phase 2+: per-player progression data (skills, stats, appearance) stored as
 * server-side attachments and synced explicitly.
 *
 * <p>Rules: nothing is written from a client packet without validation.
 */
package com.ultimatelife.common.character;
