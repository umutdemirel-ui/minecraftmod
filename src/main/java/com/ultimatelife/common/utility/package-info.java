/**
 * Shared helpers.
 *
 * <p>Side neutral helpers (resource locations, math, collections, time) used by more than one system. A helper that is only used by one system stays inside that system.</p>
 *
 * <p>Not implemented in Phase 1 - this package only documents where the system belongs so that it is
 * built in the right place later. No placeholder content ships here.</p>
 */
package com.ultimatelife.common.utility;
