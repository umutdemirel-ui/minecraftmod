/**
 * Backpack system.
 *
 * <p>Wearable backpacks with their own inventory. Inventory state is stored in capabilities and is only ever mutated on the server; the client asks for synced views through {@code com.ultimatelife.common.network}.</p>
 *
 * <p>Not implemented in Phase 1 - this package only documents where the system belongs so that it is
 * built in the right place later. No placeholder content ships here.</p>
 */
package com.ultimatelife.common.backpack;
