/**
 * Client renderers.
 *
 * <p>Entity, block entity and special renderers, plus render layer registration. Client only - a dedicated server must never load anything from here.</p>
 *
 * <p>Not implemented in Phase 1 - this package only documents where the system belongs so that it is
 * built in the right place later. No placeholder content ships here.</p>
 */
package com.ultimatelife.client.render;
